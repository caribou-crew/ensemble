{
  "name": "ensemble-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r --if-present build",
    "test:go": "go test -race ./core/... ./ensemble/... ./encore/..."
  },
  "packageManager": "pnpm@10.11.0"
}
