{
  "name": "@caribou-crew/ensemble-darwin-arm64",
  "version": "0.0.1",
  "description": "Prebuilt ensemble binary for darwin/arm64. Installed automatically as an optionalDependency of @caribou-crew/ensemble — not for direct use.",
  "os": ["darwin"],
  "cpu": ["arm64"],
  "files": ["bin/ensemble"],
  "scripts": {
    "build": "true"
  }
}
