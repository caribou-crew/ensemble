{
  "name": "@caribou-crew/ensemble-darwin-x64",
  "version": "0.0.1",
  "description": "Prebuilt ensemble binary for darwin/x64. Installed automatically as an optionalDependency of @caribou-crew/ensemble — not for direct use.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/caribou-crew/ensemble.git"
  },
  "os": ["darwin"],
  "cpu": ["x64"],
  "files": ["bin/ensemble"],
  "scripts": {
    "build": "true"
  }
}
