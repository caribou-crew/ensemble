{
  "name": "@caribou-crew/ensemble-linux-x64",
  "version": "0.0.1",
  "description": "Prebuilt ensemble binary for linux/x64. Installed automatically as an optionalDependency of @caribou-crew/ensemble — not for direct use.",
  "os": ["linux"],
  "cpu": ["x64"],
  "files": ["bin/ensemble"],
  "scripts": {
    "build": "true"
  }
}
