# Tasks: docker-build-ergonomics

## 1. Build streaming
- [x] 1.1 `runBuild` streams to the log with a bounded tail; tests for
      streaming and for the failure tail in `lastErr`.

## 2. docker.args
- [x] 2.1 `DockerPlacement.Args`, `dockerRunServiceArgs`, validation, tests.

## 3. Docs
- [x] 3.1 README; full sweep; commit.
